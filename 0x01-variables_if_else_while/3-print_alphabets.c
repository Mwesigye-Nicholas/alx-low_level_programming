#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always is 0
 *
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= z 'z'; a++)
	putchar('a');
	putchar('\n')
	for (a = 'A'; a <= 'Z'; a++)
	putchar('a');
}
