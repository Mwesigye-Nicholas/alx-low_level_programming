#include "main.h"

/**
 * print_numbers - prints numbers 0 to 9
 *
 * @void: no parameter taken
 *
 * Return: returns void
 */

void print_numbers(void)
{
	int i;

	if (i >= 48 && i <= 57)
	{
		_putchar(i);
	}
	_putchar('\n');
	return (0);
}
